PaddleOCR==3.3.2
# Для GPU версии (используется в Dockerfile):
# paddlepaddle-gpu==3.2.2 устанавливается отдельно через специальный индекс
Pillow==12.0.0
numpy==2.4.0
fastapi==0.127.0
uvicorn==0.40.0
python-multipart==0.0.21
ultralytics>=8.0.0  # Для YOLO детекции номеров контейнеров
# PyTorch с CUDA устанавливается отдельно через официальный индекс

