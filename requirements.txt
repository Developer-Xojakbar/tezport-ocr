PaddleOCR==3.3.2
# Основное venv (Windows): PyTorch/YOLO GPU. PaddleOCR GPU — в .venv-ocr (см. requirements-ocr.txt)
# Linux/Docker: paddlepaddle-gpu в том же процессе (см. Dockerfile)
paddlepaddle-gpu==3.3.1
Pillow==12.0.0
numpy<2.3.0  # Для совместимости с Python 3.10 (numpy 2.4.0 требует Python >=3.11)
fastapi==0.127.0
uvicorn==0.40.0
python-multipart==0.0.21
ultralytics>=8.0.0  # Для YOLO детекции номеров контейнеров
